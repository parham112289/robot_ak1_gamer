# اپ AK-1

این اپ درصد باتری را نشان نمی‌دهد؛ فقط در صورت ارسال داده از ربات، ولتاژ باتری را نمایش می‌دهد.

## اجرا

```bash
flutter pub get
flutter run
```

آدرس Worker در `lib/services/ak1_service.dart` قرار دارد.
