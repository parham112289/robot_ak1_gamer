# Cloudflare Worker

Secret لازم:

- `OPENROUTER_API_KEY`

Optional variables:

- `OPENROUTER_MODEL` (default: `openrouter/free`)
- `CAMERA_SNAPSHOT_URL`

کلید OpenRouter را داخل Flutter یا firmware قرار ندهید.
